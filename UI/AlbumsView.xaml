<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        x:Class="OneDriveAlbums.UI.AlbumsView">

    <CollectionView SelectionMode="Single" x:Name="Albums_CVw">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="5" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border Padding="8" Margin="6">

                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnAlbumTapped" NumberOfTapsRequired="2" />
                    </Border.GestureRecognizers>
                    
                    <VerticalStackLayout>
                        <Image WidthRequest="300" HeightRequest="300" Source="{Binding Thumbnail}" Aspect="AspectFit" />

                        <Label Text="{Binding Name}" LineBreakMode="TailTruncation" />
                    </VerticalStackLayout>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentView>
